import Box from "@mui/material/Box";
import Typography from "@mui/material/Typography";

export default function CheckInForm() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h2">CheckInForm</Typography>
      <Typography color="text.secondary">Placeholder — built in a later phase.</Typography>
    </Box>
  );
}
