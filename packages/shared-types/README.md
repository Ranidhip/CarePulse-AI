# Shared types

TypeScript interfaces shared between `apps/web` and `apps/mobile` for API
request/response shapes (e.g. `WeeklyCheckIn`, `RiskAssessment`, `Patient`).

Empty for now — populated once the API contract is finalized (next stage),
so both frontends stay in sync with the backend's Pydantic models instead
of drifting apart.
